server:
   port: 7010
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://git.d-linking.tech/Java/mec-syscfg.git
          label: hlsh-v3
          search-paths: /hlsh-config/cloud/default-settings/{profile}
          username: fengyan
          password: 20180514
          fail-fast: true
          retry:
            initial-interval: 1000
            max-attempts: 10
            max-interval: 5000
            multiplier: 1.1
eureka:
  instance:
    prefer-ip-address: true
    metadata-map:
      zone: zone-3
  client:
    region: shanghai
    availability-zones:
      shanghai: zone-3
    prefer-same-zone-eureka: true
    service-url:
      zone-3: http://dev:dev@localhost:7003/eureka/