server:
   port: 7001
spring:
  profiles: registry-1
  application:
    name: registry-service
  security:
    basic:
      enabled: true
    user:
      name: allyes
      password: allyes
eureka:
  server:
    enable-self-preservation: false
    eviction-interval-timer-in-ms: 5000
    useReadOnlyResponseCache: false
  instance:
    prefer-ip-address: true
    status-page-url-path: /actuator/info
    health-check-url-path: /actuator/health
    hostname: ${app.hostname:10.26.255.211}
    leaseRenewalIntervalInSeconds: 5
    leaseExpirationDurationInSeconds: 10
  client:
    register-with-eureka: false
    fetch-registry: false
    region: shanghai
    availability-zones:
      shanghai: zone-1,zone-2
    service-url:
      zone-1: http://allyes:allyes@${app.hostname:10.26.255.211:7001}/eureka/
      zone-2: http://allyes:allyes@${app.hostname:10.26.255.212:7001}/eureka/
      
---

server:
   port: 7001
spring:
  profiles: registry-2
  application:
    name: registry-service
  security:
    basic:
      enabled: true
    user:
      name: allyes
      password: allyes
eureka:
  server:
    enable-self-preservation: false
    eviction-interval-timer-in-ms: 5000
    useReadOnlyResponseCache: false
  instance:
    prefer-ip-address: true
    status-page-url-path: /actuator/info
    health-check-url-path: /actuator/health
    hostname: ${app.hostname:10.26.255.212}
    leaseRenewalIntervalInSeconds: 5
    leaseExpirationDurationInSeconds: 10
  client:
    register-with-eureka: false
    fetch-registry: false
    region: shanghai
    availability-zones:
      shanghai: zone-1,zone-2
    service-url:
      zone-1: http://allyes:allyes@${app.hostname:10.26.255.211:7001}/eureka/
      zone-2: http://allyes:allyes@${app.hostname:10.26.255.212:7001}/eureka/

---

server:
   port: 7003
spring:
  profiles: registry-3
  application:
    name: registry-service
  security:
    basic:
      enabled: true
    user:
      name: dev
      password: dev
eureka:
  server:
    enable-self-preservation: false
    eviction-interval-timer-in-ms: 5000
    useReadOnlyResponseCache: false
    peer-node-read-timeout-ms: 10000
  instance:
    prefer-ip-address: true
    status-page-url-path: /actuator/info
    health-check-url-path: /actuator/health
    hostname: ${app.hostname:localhost}
    leaseRenewalIntervalInSeconds: 5
    leaseExpirationDurationInSeconds: 10
  client:
    register-with-eureka: false
    fetch-registry: false
    region: shanghai
    availability-zones:
      shanghai: zone-3
    service-url:
      zone-3: http://dev:dev@${app.hostname:localhost}/eureka/
